#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'
#define AMR                  \
    ios::sync_with_stdio(0); \
    cin.tie(0);

int main()
{
    AMR
        ll t;
    cin >> t;
    while (t--)
    {
        ll n;
        cin >> n;

        cout << "mul 999999999" << endl;
        cout.flush();
        string res;
        cin >> res;
        cout << "digit" << endl;
        cout.flush();
        cin >> res;
        cout << "add " << (n - 81) << endl;
        cout.flush();
        cin >> res;

        cout << "!" << endl;
        cout.flush();
        cin >> res  ; 
    }
    return 0;
}
